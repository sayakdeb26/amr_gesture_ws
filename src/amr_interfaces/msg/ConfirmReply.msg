builtin_interfaces/Time stamp
string session_id        # must match ConfirmRequest.session_id
int32  window_id
bool   approved
string final_label       # normalized label if approved, empty if rejected
